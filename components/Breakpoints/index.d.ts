export interface Breakpoints {
    xsmall: {
        width: number;
        columns: number;
    };
    small: {
        width: number;
        columns: number;
    };
    medium: {
        width: number;
        columns: number;
    };
    large: {
        width: number;
        columns: number;
    };
}
declare const breakpoints: Breakpoints;

export default breakpoints;
