import React from 'react';
import RangeSlider from '../../../components/RangeSlider';

const Range = () => <RangeSlider defaultValue={{ from: 20, to: 80 }} />;

Range.code = '<RangeSlider defaultValue={{ from: 20, to: 80 }} />';

export default Range;
