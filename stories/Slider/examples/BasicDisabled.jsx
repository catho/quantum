import React from 'react';
import Slider from '../../../components/Slider';

const BasicDisabledSlider = () => <Slider disabled value={50} />;

BasicDisabledSlider.code = '<Slider disabled value={50} />';

export default BasicDisabledSlider;
