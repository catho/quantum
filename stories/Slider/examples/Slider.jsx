import React from 'react';
import Slider from '../../../components/Slider';

const BasicSlider = () => <Slider value={50} />;

BasicSlider.code = '<Slider value={50} />';

export default BasicSlider;
