import React from 'react';
import Slider from '../../../components/Slider';

const MinMax = () => <Slider min={30} max={50} value={40} />;

MinMax.code = '<Slider min={30} max={50} value={40} />';

export default MinMax;
