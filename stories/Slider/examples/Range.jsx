import React from 'react';
import Slider from '../../../components/Slider';

const Range = () => <Slider value={{ from: 20, to: 80 }} />;

Range.code = '<Slider value={{ from: 20, to: 80 }} />';

export default Range;
