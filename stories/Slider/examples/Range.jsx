import React from 'react';
import Slider from '../../../components/Slider';

const Range = () => <Slider value={{ from: 0, to: 100 }} />;

Range.code = '<Slider value={{ from: 0, to: 100 }} />';

export default Range;
