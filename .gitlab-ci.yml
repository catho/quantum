before_script:
  - cp .env.example .env
  - yarn install

cache:
  paths:
  - node_modules/

stages:
  - build
  - test
  - deploy
  - analysis

jest:
  stage: test
  script:
    - yarn coverage
  artifacts:
    paths:
      - coverage/lcov.info
  when: on_success
  tags:
    - style

sonar:
  stage: analysis
  dependencies:
    - jest
  script:
    - sonar-scanner
  tags:
    - style

storybook:
  stage: build
  script:
    - yarn build-storybook
  artifacts:
    paths:
      - storybook-static
  tags:
    - style
  only:
    - master

pages:
  stage: deploy
  script:
    - mkdir .public
    - cp -r storybook-static/* .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - master
  tags:
    - style

after_script:
  - yarn cache clean
